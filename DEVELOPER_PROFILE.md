# ملف مستوى المطوّر — مرجع معايرة المراجعات (UniNet)

> **الغرض من هذا الملف:** أساس ثابت تُبنى عليه مراجعات الجلسات المستقبلية، بحيث تكون المعايير مطابقة لمستوى المطوّر الفعلي — لا متشددة بمعايير Senior Architect، ولا متهاونة بمعايير مبتدئ.
>
> **تاريخ آخر تحديث:** 2026-08-06 · **الفرع وقتها:** `refactor/v1Branch` · **آخر Commit مُقيَّم:** `d58be80`
> **حالة المشروع:** قيد التطوير النشط (v1/MVP) — ليس منتجًا جاهزًا للنشر.

---

## 1. التقييم العام للمستوى

**التصنيف: Strong Junior متجه نحو Mid-level مبتدئ.**

هذا التصنيف مبني على أدلة من الكود وليس على انطباع. المطوّر يتخذ قرارات معمارية صحيحة في المسائل الصعبة، لكن ينقصه الانضباط الآلي (Systematic Discipline) في تعميم ما يقرره وفي التحقق من صحة ما ينسخه.

**المعادلة التي تلخّص مستواه:**
> جودة القرارات التصميمية **أعلى** من جودة التنفيذ التفصيلي.

هذه معادلة غير شائعة — الأغلب في هذا المستوى يكون العكس (تنفيذ نظيف لقرارات ضعيفة). وهذا يعني أن سقف تطوّره مرتفع، وأن أخطاءه قابلة للإصلاح بعادات عمل وليس بإعادة تعلّم أساسيات.

---

## 2. نقاط القوة المثبتة بالأدلة

هذه ليست مجاملات — كل بند مقترن بدليل من الكود، ويجب **عدم** إعادة اقتراحها عليه كأنها ناقصة:

| القوة | الدليل |
|---|---|
| **انضباط معماري حقيقي** | فصل الطبقات مفروض على مستوى `csproj` وليس مجرد مجلدات: `Domain` لا يرجع لغير `Contracts`، و`Application`/`DataAccessLayer` لا يرجع أحدهما للآخر. نادر في هذا المستوى. |
| **فهم عميق لـ EF Core** | فهرسة محكمة (فهارس مركّبة فريدة تطابق قواعد العمل)، لا Lazy Loading، Projection عبر `Expression<Func<>>` بدل جلب كيانات كاملة، `DeleteBehavior.Restrict` كافتراضي مع استثناء واحد مبرّر. |
| **أمان متقدّم نسبيًا لمستواه** | تدوير Refresh Token مع تخزين الـ Hash فقط (SHA-256) وربط `ReplacedByTokenId` — تنفيذ يتجاوز المتوقع من حديثي التخرج. |
| **فهم صحيح لمتى تلزم المعاملات** | `Transaction` صريحة فقط في `CreateEmployee`/`CreateStudent` (عمليات متعددة الخطوات)، وغيابها حيث لا تلزم. تمييز دقيق يخطئ فيه مطوّرون أكثر خبرة. |
| **فصل التحقق عن منطق العمل** | لا ازدواجية بين FluentValidation (شكل) وفحوصات الخدمة (وجود/تفرّد). فصل واعٍ ومتّسق. |
| **قرارات أمنية واعية** | إرجاع 404 بدل 403 عند فشل الملكية لمنع تسريب وجود المورد (موثّق بتعليقاته). وفي `GetAllCollegesPerUniversity` جعل `scope` يتقدّم على `filter` القادم من العميل — ترتيب صحيح أمنيًا. |
| **قدرة على تجاوز الحل المقترح عليه** | عند إصلاح ابتلاع الاستثناء في `UnitOfWorkRepository`، استخدم `catch ... when (ex.InnerException is SqlException { Number: 547 })` — **أنظف من الحل الذي اقتُرح عليه** (`throw;`)، لأنه يمنع التقاط الاستثناء أصلًا بدل التقاطه وإعادة رميه. مع تعليق يشرح السبب. |
| **تعلّم ذاتي موجَّه** | ملف `My_Notes_UniNet_VS_ZadByan.txt` — يقارن تصميمه (Anemic Entity + حماية عبر المسار) بمشروع آخر (Rich Entity)، ويحلل السبب. مؤشر نضج مهني حقيقي. |
| **تنظيم DI متّسق** | استخراج `PolicyExtension` و`AuthorizationHandlerExtension` من `Program.cs` تبعًا لاتفاقية المشروع القائمة. |

---

## 3. أنماط الضعف المتكررة — التشخيص الأهم

### 3.1 النمط الأول (الأخطر): تعميم ناقص للنمط المُصمَّم
هذا **أبرز نمط ضعف لديه**، وتكرر عبر ثلاث موجات متتالية:

| الموجة | ما طُبِّق | ما نُسِي |
|---|---|---|
| بناء `CollegeOwnerHandler` أولًا | `GetCollegeById` فقط | كل عمليات الكتابة |
| بعد المراجعة الأولى | `Delete` + `Add` لـ College/Department/Batch | **`Update` في الثلاثة معًا** |
| بناء الـ Handlers الأكاديمية | `GetById` + `Delete` | `GetAll*` و`Get*PerParent` و`GetByName` |

**الخلاصة التشخيصية:** يطبّق النمط على نقاط النهاية التي يفكر فيها لحظتها، ولا يُجري جردًا منهجيًا لكل النقاط المتأثرة. **`Update` تحديدًا هي الضحية المتكررة.**

**العلاج الموصى به له:** قائمة تحقق مكتوبة (Checklist) لكل نمط أمني قبل اعتباره منجزًا — صفوفها كل الـ Endpoints، وأعمدتها: `Get` / `GetAll` / `GetByName` / `Add` / `Update` / `Delete`.

### 3.2 النمط الثاني: أخطاء نسخ-ولصق غير مُتحقَّق منها
عند تكرار كتلة مشابهة، ينسخ ويعدّل جزئيًا فيسقط تعديل لازم. أمثلة مؤكدة:
- `!` مفقودة في شرط البحث (`CollegeRepository.cs:104`) بينما النسخة الأصلية صحيحة في نفس الملف (`:68`).
- ترتيب معاملات خاطئ في `IsWithinScope` (`DepartmentService.cs:49`) بينما النسخة الصحيحة موجودة في `BatchService.cs:48`.
- قاعدة `StudentNumber` منسوخة من قاعدة الهاتف بالكامل (Regex + رسالة + شرط `When`).

**الملاحظة المهمة:** في كل حالة، **النسخة الصحيحة موجودة في المشروع نفسه** — فالمشكلة ليست جهلًا بالصواب، بل غياب مراجعة ذاتية بعد النسخ.

### 3.3 النمط الثالث: تجاهل تحذيرات المُصرِّف
البناء ينتج **10 تحذيرات**، ثلاثة منها تشير لأخطاء حقيقية سبق رصدها يدويًا في المراجعة (`CS8602` على `TokenUserInfoMapper.cs:29,70` = الـ NRE المعروف، و`CS8603` على `UserTypeResult.cs:28` = إرجاع `null`). أي أن المُصرِّف كان يخبره بالأخطاء وهو لا يقرأ مخرجاته.

**العلاج:** جعل قراءة التحذيرات جزءًا من روتين ما قبل الـ Commit.

### 3.4 النمط الرابع: بقايا كود غير منظَّفة
حذف جسم دالة وترك سماتها (Attributes) معلّقة، استيرادات غير مستخدمة، تعليقات قديمة لم تعد صحيحة (`// Just for now...` بعد إضافة الفحص فعلًا)، أخطاء إملائية في المعرّفات (`Coulmns`, `UniverseId`, `Parametre`, `Requsets`).

### 3.5 فجوات معرفية (وليست أخطاء)
- **لا خبرة باختبارات الوحدة** — لا مشروع اختبارات إطلاقًا. أكبر فجوة مهارية مقابلةً بالسوق.
- **لا خبرة بالـ Logging المنظَّم** — بدأ للتو بإضافة `ILogger` في الـ Middleware (خطوة صحيحة).
- **لا خبرة بـ CI/Docker** — لم يُطرح بعد، ومؤجَّل عمدًا لما بعد V1.

---

## 4. معايير المعايرة للمراجعات المستقبلية

> **هذا هو القسم العملي — اقرأه قبل أي مراجعة قادمة.**

### 4.1 ما يُصنَّف 🔴 Critical عند هذا المستوى
- ثغرة أمنية قابلة للاستغلال فعليًا (تجاوز نطاق، تصعيد صلاحيات، IDOR).
- خطأ يكسر وظيفة معلنة كمكتملة (مثل NRE في مسار تسجيل دخول).
- خطأ توجيه/تعاقد يجعل نقطة نهاية تفعل شيئًا مختلفًا عن اسمها.
- سر مكشوف في مستودع Git.
- نمط ناقص التعميم **إن كان كل يوم تأخير يضاعف تكلفة إصلاحه** (لأنه يُنسخ لميزات جديدة).

### 4.2 ما يُصنَّف 🟠 Important
- خطأ منطقي حقيقي لكنه لا يُستغل أمنيًا (فلتر تاريخ يُرجع صفر نتائج).
- غياب فحص `null` قد ينتج استثناءً غير معالج.
- تناقض يُنتج سلوكًا مختلفًا بين مسارين متشابهين.
- قرارات مخطَّطة ومؤجَّلة عمدًا — **تُذكر كتذكير لا كلوم** (مثل تأجيل الفلاتر لبقية الكيانات).

### 4.3 ما يُصنَّف 🟡 Improvement
تكرار بنيوي، توحيد أسماء، أخطاء إملائية، استيرادات ميتة، تنظيف تعليقات، تحسينات أسلوبية. **لا يُطلب تنفيذها أثناء بناء ميزة** — تُجمَّع لتمريرة تنظيف.

### 4.4 ما يجب **عدم** طرحه إطلاقًا (تشتيت غير مبرَّر)
- CQRS، MediatR، Event Sourcing، DDD الكامل، Microservices، GraphQL.
- Redis / Distributed Caching / Message Queues — لا حِمل يبرّرها.
- AutoMapper — أسلوبه اليدوي عبر `Expression` **أفضل** أداءً لحالته، ولا يجب اقتراح استبداله.
- Generic Repository — قرار واعٍ بعدم استخدامه، موثّق في `CLAUDE.md`.
- أي Design Pattern لمجرد وجوده.

### 4.5 قواعد أسلوب المراجعة معه
1. **ابدأ بالتحقق الفعلي من الكود قبل الحكم** — لا تفترض أن ما ورد في `CLAUDE.md` أو في مراجعة سابقة لا يزال صحيحًا؛ فقد ثبت أكثر من مرة أن التوثيق تأخر عن الكود.
2. **قدّم الأدلة بأرقام أسطر** — يتعامل معها جيدًا ويتحقق بنفسه.
3. **لا تجاملة ولا تهويل** — طلب صراحةً الصراحة، ويستجيب لها بإصلاحات فعلية سريعة.
4. **اشرح "لماذا" وليس "ماذا" فقط** — يستوعب التعليل ويبني عليه (دليل: تحسينه للحل المقترح في `UnitOfWork`).
5. **حين يطلب الإرشاد دون كود** — التزم بذلك حرفيًا: وجّه، اطرح القرارات التي يجب أن يحسمها بنفسه، ولا تكتب الحل.
6. **اعترف بما أنجزه صراحةً قبل ذكر ما تبقّى** — يتابع الإصلاحات بجدية ويستحق تثبيت الصحيح منها.

---

## 5. لقطة حالة المشروع (وقت كتابة الملف)

### مُنجَز ومُثبَت
- الهيكل الأكاديمي (University → College → Department → Batch → Section): CRUD كامل.
- الهوية والمصادقة: JWT + Refresh Token + BCrypt + صلاحيات مبنية على الأدوار.
- ميزتا Employee و Student.
- تقييد متحكمات الهوية على `Super Admin` ✅ (إصلاح موجة أولى).
- Handlers الملكية الأكاديمية الأربعة + تسجيلها في DI ✅.
- `ILogger` في `GlobalExceptionHandlingMiddleware` ✅.
- إصلاح ابتلاع الاستثناء في `UnitOfWorkRepository` ✅.
- فلترة College (`BaseFilterCoulmns` + `CollegeFilter`) — **البنية صحيحة، وفيها خطآن منطقيان** (انظر §6).

### مؤجَّل بقرار واعٍ (لا يُلام عليه)
- تعميم آلية الفلترة على بقية الكيانات الأكاديمية — **مخطَّط له وسيُنفَّذ لاحقًا**.

### لم يبدأ بعد
- `Semester` / `Subject` / `SectionSubject` / `StudentResult` — موجودة في قاعدة البيانات بلا أي Repository/Service/Controller. **هذه أهم قيمة وظيفية متبقية (الدرجات).**
- `Post` / `Announcement` — مُهيكَلة في القاعدة بلا تنفيذ (قرار منتج مطلوب: تُبنى أم تُزال).
- مشروع اختبارات، تدوير مفتاح JWT، توحيد `UpdatedAt`.

---

## 6. البنود المفتوحة المرحَّلة للجلسة القادمة

مرتبة حسب الأولوية، وكلها **مُتحقَّق منها في الكود** وقت كتابة هذا الملف (بعد Commit `d58be80`):

### ✅ أُغلقت في جولة Commit `d58be80` (تحقّقتُ منها بنفسي، لا تُعِد فتحها كأنها ناقصة)
- سمات `GetCollegeByName` المعلّقة → عولجت بحذف الدالة بالكامل (ليس بإكمالها — لاحظ أن ميزة "بحث الكلية بالاسم" أصبحت غير موجودة الآن، تأكد أن هذا مقصود).
- `UpdateCollege` / `UpdateDepartment` الآن يتحققان من `IsWithinScope` بشكل صحيح.
- ترتيب معاملات `IsWithinScope` في `DepartmentService.AddDepartment` مُصحَّح.
- خطأ البحث المعكوس وخطأ فلتر التاريخ (`OR`/`AND`) في `GetAllCollegesPerUniversity` مُصحَّحان.
- `SectionAuthorizationInfo` أُكملت بالسلسلة الكاملة (University/College/Department)، و`SectionOwnerHandler` مبني ومسجَّل، و`SectionController.GetSectionById`/`AddSection`/`UpdateSection` تتحقق من النطاق الآن.
- الفلترة عُمِّمت فعليًا إلى `AcademicFilter` مشترك وطُبِّقت على Department (كان مؤجَّلًا، أُنجز أبكر من المتوقع).

### 🔧 أصلحتُها أنا مباشرة أثناء مراجعة هذه الجولة (قبل الـ Commit، بطلب صريح "تأكد من صحتها أولًا")
- `DepartmentRepository.GetAllDepartments`: فلتر `CollegeId` كان بـ `!=` بدل `==` (يستثني الكلية المطلوبة بدل تضمينها فقط).
- `CollegeRepository.GetAllColleges` (النسخة غير المرتبطة بنطاق): كانت لا تزال تحمل خطأ `OR`/`AND` في فلتر التاريخ نفسه الذي أُصلح في `GetAllCollegesPerUniversity` المجاورة — **نفس نمط "أصلح سطرًا وانسَ توأمه" الموثّق في §3.1، تكرر هنا بين دالتين شبه متطابقتين في نفس الملف.**
- `DepartmentService.UpdateDepartment`: كانت تُرجع `AlreadyExists<Department>()` عند عدم وجود القسم أو خروجه عن النطاق (يجب أن تكون `ResourceDoesntExist<Department>()`، كما في `CollegeService.UpdateCollege` المجاورة).

**ملاحظة مهمة للمرة القادمة:** الخطأين الأولين تحديدًا تكرار حرفي لنمط "نسخ-ولصق بلا تحقق" (§3.2) — نُسخت دالة تعمل بشكل صحيح، وتغيّر تفصيل صغير (عامل مقارنة، أو استُخدم Enum خاطئ) دون تشغيل/تتبع الحالة يدويًا بعد النسخ.

### مفتوح، لم يُعالَج بعد
| # | البند | التصنيف | الموقع |
|---|---|---|---|
| 1 | `GetAll*`/`Get*PerParent` في Batch/Section بلا فحص نطاق (طُبِّق على Department، لم يُعمَّم بعد على البقية) | 🟠 | `BatchController`/`SectionController` |
| 2 | `BatchController.Delete` يفحص النطاق يدويًا في المتحكم بدل `AuthorizeAsync` + Policy (تناقض مع النمط المستخدم في College/Department/Section) | 🟡 | `BatchController.cs:110-114` |
| 3 | بنود Phase 0 المتبقية: مفتاح JWT، NRE في `TokenUserInfoMapper`، تحقق `StudentNumber`، توحيد `UpdatedAt` + `BaseEntity` | 🔴/🟠 | متفرّقة |
| 4 | تنظيف: تعليق `// Just for now` (لم يعد صحيحًا)، `Coulmns`، `UniverseId`، استيرادات ميتة، 10 تحذيرات بناء (`TokenUserInfoMapper`, `UserTypeResult`, إلخ — لم تُلمَس بعد) | 🟡 | متفرّقة |

---

## 7. مؤشرات التقدّم المرصودة

للمقارنة في الجلسات القادمة:

- **سرعة الاستجابة للمراجعة:** عالية باستمرار — أغلق كل بنود الجولة الأولى (تصعيد الصلاحيات، Update بلا ملكية، ترتيب المعاملات، خطآ الفلترة الأصليان) خلال دورتين متتاليتين، وأضاف ميزة كاملة (Section ownership + تعميم الفلترة) في نفس الوقت.
- **جودة الإصلاح:** جيدة إلى ممتازة على مستوى القرار — حلّ `UnitOfWork` تجاوز المقترح جودةً، وإصلاح `GetCollegeByName` باختيار الحذف الآمن بدل الترقيع كان قرارًا معقولًا.
- **الثغرة السلوكية لا تزال قائمة، لكنها تغيّرت شكلًا:** التعميم الناقص عبر الـ *Endpoints* (مثال Phase 0.2) تحسّن ملحوظًا هذه الجولة، لكن ظهر نفس النمط الجذري (§3.1/§3.2) بشكل أدق: **نسخ دالة صحيحة إلى دالة شبيهة في نفس الملف دون تحقق لاحق** (`GetAllColleges` نسي إصلاح إخوته `GetAllCollegesPerUniversity` الذي أُصلح في نفس الجلسة تقريبًا، وفلتر `CollegeId` انعكس بعامل مقارنة خاطئ عند كتابته أول مرة). **لم يعد النمط "أنسى تطبيق الفكرة على endpoint آخر" فقط، بل صار أيضًا "أنسى إعادة فحص نسخة مجاورة كتبتها للتو".**
- **مؤشر جديد يستحق المراقبة:** بدأ يبني تجريدات صحيحة استباقيًا (`AcademicFilter` بدل `CollegeFilter` منفصل قبل أن يُطلب منه ذلك صراحة) — إشارة نضج في التخطيط، منفصلة عن نمط ضعف التنفيذ التفصيلي.

**السؤال الذي يجب طرحه في المراجعة القادمة:** هل بدأ يقرأ تحذيرات المُصرِّف (10 تحذيرات ثابتة لم تُلمَس بعد) ويشغّل/يتتبع منطق الفلاتر يدويًا بعد كتابتها مباشرة، قبل الانتقال للدالة التالية؟ إن نعم — فقد عالج نمط ضعفه الجذري، ويستحق ترقية التصنيف إلى Mid-level.
